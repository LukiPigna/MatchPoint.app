import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lucaspignataro.matchpoint',
  appName: 'com.matchpoint.app',
  webDir: 'dist'
};

export default config;
